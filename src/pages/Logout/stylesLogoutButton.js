import { styled } from 'styled-components';

export const Button = styled.button`
  font-size: 16px;
  color: #FDFDFD;
  padding: 35px;
  background: transparent;
  border: none;
  cursor: pointer;
`;
